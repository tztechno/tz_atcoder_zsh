abc082_a.zsh
##########################################
##########################################
##########################################
zmodload zsh/mathfunc
read a b
val=$(( ceil((a + b) / 2.0) ))
printf '%d' $val
##########################################
#!/bin/zsh
read -r a b
c=$(( (a + b + 1) / 2 ))
printf "%d\n" "$c"
##########################################
#!/bin/zsh
read -r a b
c=$(( (a + b + 1) / 2.0 ))
printf "%d\n" "$c"
##########################################
[python]
import math
a,b=map(int,input().split())
print((a+b+1)//2)
##########################################
